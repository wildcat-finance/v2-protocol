Synopsis schema=fiat-audit-synopsis/v1 | source=audit/rounds/fiat-fizz-4-emitter-fidelity-differential-suite.md | source_sha256=e86c6e60583afb9dbe4446a27bc452dc108579e342a3d0b5581f923c0aca7c78 | h2_count=3
## Step 1, round 1 -- 2026-09-07T10:07:35Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: oracle-derived-from-assembly=not-applicable; reference-declaration-source=not-applicable; pairing-by-name=reviewed; unpaired-emitter=not-applicable; narrowed-fuzz-domain=not-applicable; free-pointer-clobber=not-applicable; scratch-space-reuse=not-applicable; recordlogs-pairing=not-applicable; log4-arity-ceiling=not-applicable; data-region-length=not-applicable; campaign-record-provenance=not-applicable; ffi-enabled=reviewed<br><br>Not checked: Eleven ids describe a suite under `test/fizz/` that this step does not create; the step ships Markdown only. `audit_synopsis.py` is not vendored in this repository, so no synopsis sibling was regenerated and none exists to check for currency. The security suite `hexaemeron:x-ray`, `hexaemeron:solidity-auditor` and `hexaemeron:fizz` was not run as an analysis: `git diff fiat/fizz-4-emitter-fidelity-differential-suite...HEAD` changes `.horos/boundary.json`, `docs/Emitter Fidelity Runbook.md`, `docs/Emitter Fidelity Study.md` and `docs/README.md`, no `.sol` file, no test and no configuration, so all three had empty Solidity scope in this diff. No X-Ray pass over the protocol was performed and none is claimed. The pinned digest preconditions were checked and hold: `skills/x-ray/SKILL.md` at `b23bb94517805c1b8ce717d0e1e0282b0b5c14c7b16f4c32e73940292d3d4a41`, `lib/xray_reuse.py` at `23e628b7ffc963000ace6b081c57de267267c37108527ef7d3a092dbf0fcab10`, `skills/solidity-auditor/SKILL.md` at `1c1cf4e99d042e7aadc56b622d97a07d3286f4786838a05510697c814d1e983f`, each matching `PROMISES.md`. Pull request bodies for 93, 62, 36 and 35 cited in study section 2 were not fetched; the `release/v2.5` claims at `bea503c2736d47de7fd34130c64f10783dc35b39` (29 emitters, `log4`, two renames, one undeclared emitter) were not verified against that branch. The `oracle-derivation-ms` and `transcribed-oracle-bytes` metric cells in the design matrix were not re-measured.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | medium | docs/Emitter Fidelity Study.md | Assumption 2 states `forge test` "reports the same lint as a warning and exits zero" and that "Every step exit therefore rests on `forge test`". False at this ref: `forge test --block-timestamp $(date +%%s)` exits 1 with 795 passed, 2 failed, 797 total, the failures being `testFail_redeem` and `testFail_withdraw` in `test/vault/Wildcat4626WrapperStandard.t.sol`. The runbook states this correctly at lines 47 to 53, so the two byte-pinned deliverables disagree on the exit command every later step rests on. | open |<br>| S1-R1-02 | medium | docs/Emitter Fidelity Study.md | Section 11 declares the Elenchus runner contract as `forge test --match-path 'test/fizz/*.t.sol' --json > {report}` with report format "Foundry's JSON test output". The runbook declares `sh -c "forge test --match-path 'test/fizz/*.t.sol' --junit > {report}"` with format `forge-junit-v1` and report file `.hexaemeron/test-reports/step-<n>.json`. Elenchus accepts only `unittest-json-v1`, `forge-junit-v1` and `node-test-json-v1` (`skills/elenchus/scripts/elenchus.py:29`), so the study's declared format would be refused with "the declared report format is not supported". The runbook's contract is the one Fiat binds and it works, verified below; the study's does not. | open |<br>| S1-R1-03 | medium | docs/Emitter Fidelity Study.md | Study success criterion 3 and runbook step 3 exit 4 both require `python3 .hexaemeron/measure_design.py --table`. `.hexaemeron/.gitignore` line 1 is `*`, so that script, `design-evidence.json` and `reports/` are untracked and ship with no delivered branch; `git ls-files .hexaemeron` is empty. The script runs and exits 0 for the operator now, but step 3's exit and the study's criterion 3 cannot be reproduced by anyone who checks out the branch, and study section 7 lists these ignored paths as sources. | open |<br>| S1-R1-04 | low | docs/Emitter Fidelity Study.md | Section 3 attributes the `forge build` failure to "`Error: Lint failed` on an `unsafe-typecast` in `src/market/WildcatMarketBase.sol:45`", and assumption 2 repeats the attribution. The behaviour holds -- `forge build` exits 1 with `Error: Lint failed` -- but the location and diagnostic level do not: forge 1.7.1 emits 133 `warning[...]` lints and zero `error[...]` lints; `WildcatMarketBase.sol` carries only two `block-timestamp` warnings, at 430:9 and 502:9; line 45 is `address public immutable sentinel;`, which contains no typecast. An engineer sent to that line finds nothing. | open |<br><br>Leads not pursued: No fixes commit was made and no Elenchus verdict is therefore due. All four findings sit in `docs/Emitter Fidelity Study.md`, which runbook step 1 exit 1 requires to be byte-identical to the receipted `.hexaemeron/study.md`; `cmp -s` exits 0 for the study at `4e425fd3099883aaff96a57cf433a4ae603ee924462947c3cffd79cf676bbc09` and for the runbook at `31fb387c9d97585c3310bc933d35aa8a16a944c67a94ed25dcd33ab7e8f904c4`, both matching the brief. Repairing any of them by editing the tracked copy would break that byte identity, and the receipted source is controller-pinned and amendable only through the controller, so each finding is left open for a study amendment rather than fixed here. What passed: `docs/README.md` gains exactly two index lines and all eleven percent-encoded links resolve, the two new ones included; the design-lock block's schema `protasis-design-evidence/v1`, digest `af3f9fd53c62692eb01a47811865029850a1ddd52e800b6a303bc2ae94873369` and candidate `mirror-emit` all match `.hexaemeron/design-evidence.json`, whose `selection.rule` is `unique-frontier`; imprimatur scores 100.0 with 0 defects on all three changed documents; protasis reports clean on study and runbook; `horos.py check .` reports the boundary matches the tree, and its two entries are `deployments/` and `yarn.lock` as study section 7 states. Repository claims verified against the tree, not accepted: 27 assembly emitters, 22 in `src/libraries/MarketEvents.sol` and 5 in `src/spherex/SphereXProtectedEvents.sol`, with no `log0` to `log4` opcode anywhere else in `src/`; arities 9 `log1`, 9 `log2`, 4 `log3` and 5 `log1` respectively; 23 declarations in `src/interfaces/IMarketEventsAndErrors.sol` spanning lines 81 to 170 and 5 in `src/spherex/SphereXConfig.sol` at lines 42 to 46; `AccountSanctioned` declared at line 127 with no emitter and no `emit`, a dead declaration; `emit_SanctionedAccountAssetsQueuedForWithdrawal` taking `uint32 expiry` against a declared `uint256 expiry`; `main` at `f5a26146987926f4811b72a795d662813dedfe85` differing from `v2.1.0` in five files and zero `.sol` files, so Solidity-identical as claimed; 7 wrappers in `test/libraries/wrappers/`; 9 `vm.expectEmit` in `test/spherex/SphereXConfig.t.sol`; `foundry.toml` setting `ffi = true`, `solc = "0.8.25"`, `evm_version = 'cancun'`, `bytecode_hash = 'none'` and `[profile.default.fuzz] runs = 1000`; `medusa` 1.5.1 and `echidna` on PATH. `measure_design.py --table` exits 0 and reports `topic0_mismatches`, `indexed_mismatches`, `data_word_mismatches` and `unpaired` all empty across 27 emitters, so study section 4's static agreement holds. The runbook's Elenchus runner contract was executed rather than read: `sh -c "forge test --match-path 'test/LogTest.sol' --junit > report"` writes a `<testsuites tests="1" failures="0" errors="0">` document that `parse_forge_report` accepts, and forge writes the XML to stdout while lint diagnostics go to stderr, so the redirect captures a clean report. Against the declared `test/fizz/*.t.sol`, which does not exist at this step, the same command exits 0 and writes 76 bytes of plain text beginning "No tests found in project!", which is not XML and which Elenchus classifies as `inconclusive` -- the fail-closed behaviour the runbook already promises for a malformed report, so it is recorded as behaviour rather than as a finding. Three leads were seen and set aside: `foundry.toml` carries an unknown `fuzz_runs` key under `[profile.coverage]` that forge warns about on every invocation, which predates this run and belongs to no step here; `src/spherex/SphereXProtectedRegisteredBase.sol:153:39` produces a persistent `error: unresolved symbol locals` diagnostic on every `forge test` without failing the run, unexamined because no step in this runbook touches `src/`; and the runbook names report file `.hexaemeron/test-reports/step-<n>.json` for a `forge-junit-v1` format whose content is XML, a naming mismatch that Elenchus does not act on because it dispatches on the declared format rather than the extension, left unraised for that reason. A Sapheneia bounded durable-record pass was applied to this record: the host structure and the protected inventory were frozen first, the shortened candidate was compared to the source item by item, and every finding, severity, status, identifier, path, digest, line anchor, count, negative result, unknown and unpursued lead was confirmed present with no claim, qualification or verdict changed.
## Step 1, round 2 -- 2026-09-07T10:14:55Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: oracle-derived-from-assembly=not-applicable; reference-declaration-source=not-applicable; pairing-by-name=reviewed; unpaired-emitter=not-applicable; narrowed-fuzz-domain=not-applicable; free-pointer-clobber=not-applicable; scratch-space-reuse=not-applicable; recordlogs-pairing=not-applicable; log4-arity-ceiling=not-applicable; data-region-length=not-applicable; campaign-record-provenance=not-applicable; ffi-enabled=reviewed<br><br>Not checked: the round re-checks the four round 1 findings and nothing else. The diff since b482447725bb3e002e9f0797f401461d887b3673 is the two tracked documents and the regenerated boundary, with no Solidity, so the security suite again had nothing to analyse and no X-Ray pass is claimed. Eleven risk ids describe a suite under `test/fizz/` that step 1 does not create. This round was run by the controller session inline, as the audit loop permits for a re-check of one small diff.<br><br>Elenchus verdict: unguarded<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| S1-R1-01 | medium | docs/Emitter Fidelity Study.md | assumption 2 said `forge test` reports the lint as a warning and exits zero, while it exits 1 at this ref on two pre-existing `testFail` cases | fixed in 6d2bc7eb02e5bd4cff53893650a0d7299a062a7b |<br>| S1-R1-02 | medium | docs/Emitter Fidelity Study.md | section 11 declared an Elenchus runner contract in a report format the tool does not accept | fixed in 6d2bc7eb02e5bd4cff53893650a0d7299a062a7b |<br>| S1-R1-03 | medium | docs/Emitter Fidelity Study.md | success criterion 3 rested on a script under an ignored directory that no delivered branch carries | fixed in 6d2bc7eb02e5bd4cff53893650a0d7299a062a7b |<br>| S1-R1-04 | low | docs/Emitter Fidelity Study.md | the `forge build` failure was attributed to a file and line that contains no typecast | fixed in 6d2bc7eb02e5bd4cff53893650a0d7299a062a7b |<br><br>Leads not pursued: round 1's three stand and none is this step's. `foundry.toml` carries an unknown `fuzz_runs` key under `[profile.coverage]` that forge warns about on every invocation; `src/spherex/SphereXProtectedRegisteredBase.sol:153:39` produces a persistent `error: unresolved symbol locals` diagnostic on every `forge test` without failing the run; and the runbook names a `.json` report file for a `forge-junit-v1` format whose content is XML, which Elenchus does not act on because it dispatches on the declared format. Two protocol facts round 1 established are carried here because the suite this run builds is what would settle them, and neither is a finding against this step: `AccountSanctioned` is declared at `src/interfaces/IMarketEventsAndErrors.sol:127` with no emitter and no `emit`, a dead declaration; and `emit_SanctionedAccountAssetsQueuedForWithdrawal` takes a `uint32 expiry` against a declared `uint256 expiry`, which is exactly the emitter-versus-declaration divergence class the suite exists to catch. Evidence for this round: the controller amended the study on 2026-09-07 from sha256 4e425fd3099883aaff96a57cf433a4ae603ee924462947c3cffd79cf676bbc09 to 6e3ac9ba86dd6c465e274c466c611dc7ceeeb77571c5e71e76e75a194a9d06a8 and the runbook from 31fb387c9d97585c3310bc933d35aa8a16a944c67a94ed25dcd33ab7e8f904c4 to 40fae701f63d3c2fcd9c06e233934daed40394cede49da34f3ef2b8448e06daf, and both tracked copies follow byte for byte, proved by `cmp -s`; `protasis.py` reports clean on both published paths; `imprimatur.py` scores 100.0 with 0 defects on both; `horos.py check .` reports the boundary matches the tree; `forge test --block-timestamp $(date +%%s)` reports the same two pre-existing failures, `testFail_redeem` and `testFail_withdraw`, with 795 passing, matching the starting ref; `git diff --check` exits 0; `elenchus.py --ref 6d2bc7eb02e5bd4cff53893650a0d7299a062a7b` under the step's runner contract exits 0 with status `unguarded` and detail `the commit changed no test files`, which is the right reading for a documentation correction.
## Step 1, round 3 -- 2026-09-07T10:17:07Z<br><br>Audit schema: fiat-audit-round/v2<br><br>Covered: oracle-derived-from-assembly=not-applicable; reference-declaration-source=not-applicable; pairing-by-name=reviewed; unpaired-emitter=not-applicable; narrowed-fuzz-domain=not-applicable; free-pointer-clobber=not-applicable; scratch-space-reuse=not-applicable; recordlogs-pairing=not-applicable; log4-arity-ceiling=not-applicable; data-region-length=not-applicable; campaign-record-provenance=not-applicable; ffi-enabled=reviewed<br><br>Not checked: the diff since round 2's record commit 2d377dee237efe93cd3bdfbbab143735467e07aa is that round's own record and its synopsis, with no Solidity and no tracked document, so the security suite again had nothing to analyse and no X-Ray pass is claimed. The eleven ids describing a suite under `test/fizz/` remain not applicable until step 2 creates it. This round closes step 1's audit loop and was run by the controller session inline.<br><br>Elenchus verdict: null<br><br>| id | severity | file | finding | status |<br>| --- | --- | --- | --- | --- |<br>| -- | -- | -- | none | -- |<br><br>Leads not pursued: the three from round 1 stand unchanged and none belongs to this step, and the two protocol facts carried in round 2 stand as work for the suite rather than as findings. `foundry.toml` carries an unknown `fuzz_runs` key under `[profile.coverage]`; `src/spherex/SphereXProtectedRegisteredBase.sol:153:39` produces a persistent `error: unresolved symbol locals` diagnostic that does not fail the run; and the runbook names a `.json` report file for an XML format, which Elenchus does not act on because it dispatches on the declared format. Evidence for this round: `cmp -s` holds both tracked documents against the receipted artefacts at sha256 6e3ac9ba86dd6c465e274c466c611dc7ceeeb77571c5e71e76e75a194a9d06a8 and 40fae701f63d3c2fcd9c06e233934daed40394cede49da34f3ef2b8448e06daf; `protasis.py` reports clean on both published paths; `imprimatur.py` scores 100.0 with 0 defects on both; `horos.py check .` reports the boundary matches the tree; `audit_synopsis.py --check .` exits 0; `forge test --block-timestamp $(date +%%s)` reports the same two pre-existing failures with 795 passing; `git diff --check` exits 0.
