# v2.5 Consolidated Pre-Audit Delta

Baseline: branch `main` (the deployed, audited v2 protocol).
Candidate: `release/v2.5` HEAD plus the rounding-interaction fixes staged for commit.
Scope: `git diff main HEAD -- src/` — 47 files, ~4,000 insertions over 65 commits.

This document consolidates three independent review streams run over the full
delta as a single artifact: two per-subsystem correctness reviews (codex), one
cross-change interaction review (multi-agent workflow, 31 agents), plus a
mechanical-proof pass over the resulting fixes. It is intended as the
auditor-facing inventory of what changed, why, what was found, and what
evidence backs the current state.

## 1. Executive summary

- Three features: **revolving credit markets** (WildcatMarketRevolving +
  HooksFactoryRevolving), **periodic-term hooks** (recurring withdrawal
  windows + proposed/permissionless APR reductions), and a **lens rework**
  (facade + helper contracts, ABI-stable).
- One deliberate hardening with protocol-wide reach: transfer/deposit/
  withdrawal scaling changed from half-up (`rayDiv`/`scaleAmount`) to
  **floor** (`scaleAmountDown`). Every regression found in this review
  traces to that change composing with exact-boundary consumers; all are
  fixed (section 3) with mechanical proofs and regression pins.
- Remediation series CR-01..CR-07 and the CAF audit responses are folded in
  and tagged per file below.
- **Deployed singletons are untouched**: WildcatArchController,
  WildcatSanctionsSentinel, WildcatSanctionsEscrow, and the SphereX base have
  zero bytecode-affecting changes (independently confirmed twice).
- The 4626 wrapper was rebuilt for floor-rounding markets and its factory is
  now a generation-routing facade; covered by its own review cycle and the
  EIP-4626 audit-scope note.

## 2. Change inventory

### 2.1 Core money paths (src/market, src/libraries, src/types)

| File | Consolidated changes | Rationale | Class |
|---|---|---|---|
| `src/libraries/FeeMath.sol` | NatSpec return tag fix only. | `7088858` style/comments | comment/style |
| `src/libraries/LibERC20.sol` | Pragma narrowed from `^0.8.4` to `>=0.8.20`. | `7088858` style/stale cleanup | comment/style |
| `src/libraries/MarketErrors.sol` | Added APR-reduction errors; removed stale buyback errors; fixed APR error comment. | `aa6d51d`, CR-03; RCF merge; style | new-code + comment/style |
| `src/libraries/MarketEvents.sol` | Removed stale sanctioned-assets-sent-to-escrow emitter. | `7088858`, CAF-03 cleanup | comment/style |
| `src/libraries/MarketState.sol` | Added `scaleAmountDown`; `withdrawableProtocolFees` now saturates if unclaimed withdrawals exceed assets. | RCF merge; v2.5 rounding | behavioral |
| `src/market/WildcatMarket.sol` | Deposits floor normalized-to-scaled conversion; borrow/repay/close made virtual and call new extension hooks; close liquidity subtraction made saturating; sanctioned nuke path documented as ordinary withdrawal queue. | RCF; CR-02; v2.5 rounding; CAF-03 | behavioral + new-code |
| `src/market/WildcatMarketBase.sol` | Version returns `2.5`; added `scaledTransferRounding()` marker; `withdrawableProtocolFees()` gets `nonReentrantView`; added virtual seams; state accrual dispatches `_updateScaleFactorAndFees`; batch payments floor liquidity scaling and no-op on zero scaled burn. | v2.5 rounding; RCF; CAF-09; CR-07 | behavioral + new-code |
| `src/market/WildcatMarketConfig.sol` | Factored APR/reserve validation/write helper; added permissionless periodic APR-reduction execution gated by new hook flag; unchanged reserve ratio is re-applied with old-ratio reserve check. | `aa6d51d`, `04e6bc6`, CR-03 | behavioral + new-code |
| `src/market/WildcatMarketRevolving.sol` | New revolving market: commitment fee immutable, `_drawnAmount`, borrow/repay/close overrides, drawn-principal clamp, and custom interest accrual: commitment on supply plus APR on `min(drawnAmount,totalSupply)`. | RCF feature; CR-02; CR-07 | new-code |
| `src/market/WildcatMarketToken.sol` | Transfer scaling changed to `scaleAmountDown`; comments added. | v2.5 rounding | behavioral + comment/style |
| `src/market/WildcatMarketWithdrawals.sol` | Queue withdrawal scaling changed to floor; repay-and-process made virtual, reads `totalAssets()` once through repay seam, and uses saturating liquidity math; comments added. | v2.5 rounding; CR-07 | behavioral + gas-only |
| `src/types/HooksConfig.sol` | Added flag bit/read method for execute-pending-APR-reduction; fixed borrow hook size constant reference; clarified execute-withdrawal normalized amount and several comments/constant names. | CR-03; `357de0e`; `7088858` | new-code + comment/style |
| `src/types/LenderStatus.sol` | Comment fix only. | `7088858` | comment/style |
| `src/types/RoleProvider.sol` | Comment fix only. | `7088858` | comment/style |

Post-review additions (rounding-interaction fixes, staged):

| File | Change | Rationale | Class |
|---|---|---|---|
| `src/libraries/MarketState.sol` | Added `maxScaledSettleableAmount`: largest scaled amount whose floor-priced cost fits in available liquidity (`mulDivUp(n + 1, RAY, sf) - 1`). | Interaction finding 1 (close settlement / batch stranding) | behavioral |
| `src/market/WildcatMarketBase.sol` | Both `_applyWithdrawalBatchPayment` sites use the settleable capacity instead of `scaleAmountDown`. | Interaction finding 1 | behavioral |
| `src/access/{Open,Fixed,Periodic}TermHooks.sol` | Minimum-deposit check compares in scaled units (both sides floored identically); zero-minimum short-circuit unified. | Interaction finding 2 | behavioral |
| `src/market/WildcatMarket.sol` | caf-03 comment names periodic windows as accepted deferral. | Interaction finding 3 (accepted) | comment |

### 2.2 Access control and periphery (src/access, factories, singletons, interfaces)

- `src/access/BaseAccessControls.sol`: [behavioral] adds `InvalidCredentialTimestamp` for zero/future push credentials; [behavioral] replaces direct `isPullProvider()` call with defensive staticcall, classifying only clean `true` as pull; [behavioral] threads two pull-provider skip indices through hooks-data validation and expired-credential refresh; [behavioral] `getLenderStatus` uses the same refresh-then-loop flow with duplicate-provider skip. Log rationale only shows `feat/rcf-v2` merge plus style/stale-code cleanup.

- `src/access/OpenTermHooks.sol`: [behavioral] adds `InvalidAccessConfiguration` guard when queue-withdrawal access is enabled without deposit access and transfer access/disablement; [behavioral] `onQueueWithdrawal` now rejects unhooked markets before access checks. Log rationale: merge plus style/stale-code cleanup.

- `src/access/FixedTermHooks.sol`: [behavioral] adds the same invalid access-configuration invariant, but keyed off the borrower-requested queue-withdrawal access flag before required fixed-term queue hooks are merged. Log rationale: merge plus style/stale-code cleanup.

- `src/access/PeriodicTermHooks.sol`: [new-code] new periodic-term access template. It stores per-market access flags, first withdrawal window, period duration, withdrawal-window duration, minimum deposit, transfer disablement, and closed state. It enforces withdrawal windows on queue withdrawal unless the market is closed. It supports borrower-proposed APR reductions with a fixed response window, expiry after one period, cancellation on overwrite/increase/close, and execution only after the response window with no unpaid withdrawals. It adds a permissionless market entrypoint for executing matured APR reductions. It tracks known lenders through deposits/transfers so withdrawal access remains usable when configured safely. Log rationale includes `feat: re-add periodic term hooks initial code`, explicit first window/gated APR reduction, testnet min durations, stale/closed APR-change fixes, public APR execution, CR-01 invalid access config, and CR-03 explicit hook flag.

- `src/access/IHooks.sol`: [comment/style] hook callback documentation only.

- `src/access/IRoleProvider.sol`: [comment/style] `getCredential` documentation only.

- `src/access/MarketConstraintHooks.sol`: [comment/style] doc correction around constraint behavior.

- `src/access/ProviderStructs.sol`: [comment/style] role-provider input comment wording only.

- `src/HooksFactory.sol`: [behavioral] paginated getters now return an empty array for boundary-empty pages after clamping; [behavioral] market deployment now verifies `create2WithStoredInitCode` returned the predicted market address and can revert `MarketDeploymentAddressMismatch`; [behavioral] `pushProtocolFeeBipsUpdates` now clamps end, reverts `InvalidPaginationRange` on `start > end`, and no-ops on `start == end`. Log rationale: HooksFactory hardening and CR-06 pagination cleanup.

- `src/IHooksFactory.sol`: [behavioral/interface] declares `MarketDeploymentAddressMismatch` and `InvalidPaginationRange`; [comment/style] clarifies pagination `end`, `onCreateMarket`, and fee-push docs. Log rationale: hardening, ambiguous `len`, style/stale cleanup.

- `src/HooksFactoryRevolving.sol`: [new-code] revolving-market factory copied from `HooksFactory` with intentional RCF extensions. It deploys markets from revolving init code, decodes factory-owned `marketData` as version plus `commitmentFeeBips`, validates fee bips, and stores commitment fee in transient deployment data. It exposes `getRevolvingMarketCommitmentFeeBips()` for the revolving market constructor. It preserves template/instance registries, origination fees, borrower checks, salts, blacklist checks, `onCreateMarket`, CREATE2 address verification, market indexing, and protocol-fee push pagination. Log rationale: RCF merge, `create2WithStoredInitCode`, CR-06 pagination, gas/docs/code-dedup.

- `src/IHooksFactoryRevolving.sol`: [new-code] revolving factory interface with market-data errors, commitment-fee getter, and deploy functions carrying `marketData`.

- `src/WildcatArchController.sol`: [comment/style] documentation only, including CAF-13/CAF-16 notes; no behavioral source change.

- `src/WildcatSanctionsSentinel.sol`: [comment/style] removes one stale salt comment; no behavioral source change.

- `src/interfaces/IMarketEventsAndErrors.sol`: [behavioral/interface] removes stale buyback errors and old sanctions escrow event; adds APR-reduction errors and current sanctions withdrawal/escrow events. Log rationale: stale-code cleanup and APR-reduction public execution.

- `src/interfaces/IWildcatMarketRevolving.sol`: [new-code] minimal revolving market view interface for `commitmentFeeBips()` and `drawnAmount()`.

- `src/spherex/SphereXConfig.sol`: [comment/style] comment whitespace only.

- `src/WildcatSanctionsEscrow.sol`: no scoped diff.

## 3. Findings and resolutions

### 3.1 Fixed, with mechanical proofs and regression pins

**F1 — Batch settlement vs exact-debt close (HIGH).** Floor capacity
(`scaleAmountDown(availableLiquidity)`) under-counted by one scaled token
whenever `frac(S * sf / RAY)` fell in `(0, 0.5)`, making `closeMarket`
revert `CloseMarketWithUnpaidWithdrawals` on fully-funded closes with the
supply queued, and permanently stranding one scaled token of full-withdrawal
batches on already-closed markets (repayment is impossible there). Both
reproduced on-EVM before the fix.
*Fix:* `maxScaledSettleableAmount` — the largest `k` with
`floor(k * sf / RAY) <= n`. Proven (exact-integer search, 40k+ random pairs
plus edge sweeps, zero counterexamples): never pays more than available;
exactly maximal; widens the old capacity by at most one scaled token; and the
close-settlement identity `k_max >= S` holds for all supplies and scale
factors in both rounding branches of the debt normalization. Regression pins:
`test/integration/RoundingRegressionRepro.t.sol` (with stranding-window
vacuity guards) and the matrix invariant unwind at **zero** stranding
tolerance.

**F2 — Exact-minimum deposits (MEDIUM).** Floor-scaled deposits round-tripped
to `minimumDeposit - 1` wei in the hooks' half-up recomputation, rejecting
exact-minimum tenders on all three templates once any interest accrued.
*Fix:* compare in scaled units. Proven: every tender `>= minimumDeposit` is
accepted at any scale factor; the acceptance band below the minimum is
bounded by one scaled token (worst case ~5.19e6 wei at the maximal uint112
scale factor); tiny-minimum edge backstopped by the market's
`NullMintAmount`. Boundary pinned exactly (smallest accepted tender and
largest rejected tender) across all three templates at accrued scale factors.

### 3.2 Accepted behavior (documented, not fixed)

**F3 — Sanctions quarantine deferred by withdrawal gates.** `nukeFromOrbit`
routes through the ordinary withdrawal path (caf-03 decision), so periodic
windows defer quarantine until the next window — recurring per period, up to
365 days — as a long fixed term already could. Debated and accepted;
documented in the caf-03 comment and Known Issues; pinned by
`test/integration/SanctionsScenarios.t.sol`.

**F4 — Credential wrong-denial vs v2 baseline (LOW).** When hooksData names a
pull provider and the presented credential fails validation, the provider's
`getCredential` is no longer consulted as a fallback. Fail-closed, never
wrong-approval, retryable by resubmitting without the bad hooksData.

### 3.3 Clean surfaces (verified, not just unexamined)

Accounting invariants (scale-factor monotonicity, totalDebts composition,
batch conservation, borrowable bounds); every remaining rounding call site in
core traced with rounding direction judged correct; hook-calling assembly
layouts vs IHooks; the revolving virtual seams under every interleaving
including two-segment batch-expiry accrual; the three templates'
InvalidAccessConfiguration differences (intentional); HooksFactoryRevolving
vs HooksFactory divergences (enumerated, all intentional); interfaces vs
implementations; version-string consumers (`'2.5'` keeps the first-byte
`'2'` contract).

## 4. Accepted behavioral deltas vs the audited v2

1. Transfer/deposit/withdrawal scaling rounds **down** (was half-up); the
   rounding wei always disadvantages the acting party or favors the market.
   Markets declare this via `scaledTransferRounding()`.
2. `version()` returns `'2.5'` (was `'2'`); first byte preserved.
3. Batch payments settle up to the exact floor-priced capacity (F1 fix) —
   strictly more settlement than v2.5-pre-fix, at most one scaled token more
   than the naive floor, never exceeding available liquidity.
4. Minimum-deposit enforcement is scaled-space (F2 fix) with a sub-one-scaled-
   token acceptance band under the nominal minimum.
5. Credential fallback narrowing (F4). 6. Periodic quarantine deferral (F3).
7. Revolving markets accrue `commitmentFee + APR * min(drawn, supply)/supply`
   rather than APR on full supply.

## 5. Evidence

- Full suite: 1,163 tests, 0 failures (includes 60+ integration tests over
  the {Open,Fixed,Periodic} x {Standard,Revolving} matrix with a
  closed-form interest oracle covering delinquency penalties).
- Stateful fuzz: matrix invariants 18/18 at 10,000 runs x depth 100
  (pre-fix baseline) and at 5,000 x 100 with zero stranding tolerance
  (post-fix); CAF12 invariants 8/8 at 10,000 x 100; full suite at 10,000
  fuzz runs, 0 counterexamples.
- Coverage: 95.6% lines / 80.6% branches over src/ (procedure in
  docs/BACKLOG.md; requires the coverage-spherex patch).
- Arithmetic proofs: 10/10 items proven for the F1/F2 fixes (exact-integer
  search, edge sweeps at sf = RAY, uint112.max, 1-wei amounts, uint104
  boundaries).
- Reviews: 2x codex subsystem passes, 3x multi-agent interaction/fix passes
  (31, 15 agents), all findings resolved or explicitly accepted above.

## 6. Coverage appendix: enumerated uncovered branches

Final coverage: **98.35% lines, 96.32% branches** (498/517) over `src/`,
1,298 tests. Every uncovered branch is enumerated and justified below; there
is no unexamined dark territory. Categories: **[D]** deployment-time only,
**[P]** proven unreachable (arithmetic identity or dominating guard),
**[E]** external-dependency revert-side (documented in Known Issues),
**[U]** unused library-completeness overload, **[A]** instrumentation
attribution artifact (both behaviors asserted by passing tests).

| Branch | Class | Justification |
|---|---|---|
| WildcatMarketBase.sol:162 | D | Constructor-time factory parameter fetch failure; a failure means no market deploys. |
| WildcatMarketBase.sol:280, 796, 819 | E | Sanctions-oracle call revert-sides; bubbling is deliberate. See Known Issues, "Chainalysis sanctions oracle liveness". |
| WildcatMarketBase.sol:709 | P | Fully-paid batches are skipped by every caller before reaching the payment helper. |
| WildcatMarket.sol:173, 174 | P | Internal `_repay` guards; `closeMarket` proves amount > 0 and market open before calling. External `repay`'s own copies are covered. |
| WildcatMarket.sol:295 | P | `CloseMarketWithUnpaidWithdrawals` is unreachable when the borrower can cover debts, post `maxScaledSettleableAmount` fix — the fix's purpose, independently confirmed. Insufficient borrower funds revert earlier in the transfer. |
| HooksFactoryRevolving.sol:535 | P | Internal template-exists recheck dominated by the `HooksInstanceNotFound` guard in every caller. |
| LibStoredInitCode.sol:141 | U | create2 failure side of the constructor-args overload; no src/ caller uses that overload. |
| Wildcat4626Wrapper.sol:294, 359, 391 | P | Ceil/floor round-trip identities for scaleFactor >= RAY (proven by exact-integer search); each pinned positively. |
| Wildcat4626Wrapper.sol:460 | A | `_beforeTokenTransfer` zero-amount early return; line-hit data shows the attribution gap while the asserting test passes. |
| OpenTermHooks.sol:281, FixedTermHooks.sol:345 | A | Phantom records on compound-condition closing parens; all three behavior combinations (known-lender skip, valid-hooksData pass, revert) asserted by passing tests. |
| PeriodicTermHooks.sol:748 | A | APR-increase arm executes 4x in the unit suite and at market level; the false arc is attributed to the covered `else if` record. |
| SphereXProtectedRegisteredBase.sol:170, 264 | E | Engine pre/post-call failure arms; require a hostile engine mid-flight. Third-party middleware; engine-on and engine-off paths both covered. |

## 7. Deferred (docs/BACKLOG.md)

v2.5 deployment scripts (incl. 4626 facade wiring), prettier-plugin upgrade,
lens Core/Live unused immutables, hoisting the tripled minimum-deposit check
into MarketConstraintHooks, removal of the now-unused half-up
`MarketState.scaleAmount` and its unit test.
