# V2.5 pre-freeze verification receipt

This receipt records a local pre-commit run on 2026-08-31 UTC. It is evidence
for the staged review packet, not the final release attestation. Repeat it from
a clean recursive checkout after the source-freeze and packet commits exist.

## Identity

- Source candidate: `af33a82690c7935e23cd0a988a0b9133c5359614`
- Candidate `src/` tree: `27a8053e2c0367977a7bebf383f698bc894a6f2c`
- Inventory SHA-256:
  `75c4723b8d026fd8a9266d0c4afd7d439b3d54a7fbe051a06f238ce4172f484b`
- Uncommitted test patch SHA-256, produced by
  `git diff --binary HEAD -- test`:
  `b5cd67e222fbfb634ff5720c29869fc8c87d22e99974ab0c0a65fb6240c21ac6`
- Foundry: `1.7.1` (`4072e48705af9d93e3c0f6e29e93b5e9a40caed8`)
- Solidity: `0.8.25+commit.b61c2a91`
- Build: Cancun, via IR, optimizer 44, bytecode hash disabled, CBOR metadata
  disabled

## Results

| Check | Result | Receipt detail |
| --- | --- | --- |
| `FOUNDRY_PROFILE=default forge build --force src` | Pass | 118 files compiled with solc 0.8.25 |
| `FOUNDRY_PROFILE=default forge test --summary` | Pass | 692 passed, 0 failed, 0 skipped; log SHA-256 `15082db1a3be5e41eadad4e2219c1376f9d968ec05f4f09e99c05e0481c154c8` |
| `FOUNDRY_PROFILE=default yarn test:fixed --summary` | Pass | 692 passed, 0 failed, 0 skipped; timestamp `1724284800`, seed `0x5eed`; log SHA-256 `f0b972548ea342463177f4566ee3f01ce7c14164ac956b3b00fc4f188b15c89c` |
| `FOUNDRY_PROFILE=deploy forge test --summary` | Pass | 692 passed, 0 failed, 0 skipped; log SHA-256 `43cb66dd5a9090f0701bf385ef9d1022f95df1fdc2afd40cd4efabbcd74a7cc0` |
| `FOUNDRY_PROFILE=deploy forge build --force --sizes src` | Pass | Largest runtime/initcode: `WildcatMarketRevolving`, 21,911 / 23,429 bytes; runtime margin 2,665 bytes |
| Contract inventory generation and `--check` | Pass | 108 source units, 115 ABI-bearing declarations; pinned commit and source hashes match |
| Changed/new Markdown local links | Pass | All 67 local targets and anchors resolve |
| `git diff --check` | Pass | No whitespace errors |
| `yarn lint:check` | Baseline waiver | Current and `release/v2.5` fail on the same 40 Prettier files with byte-identical output |
| Standalone Solhint | Pass with warnings | 0 errors and 22 `max-line-length` warnings; `release/v2.5` has 26 |

Each of the nine `MarketMatrixInvariantTest` properties ran 2,000 sequences of
depth 30: 60,000 calls per property with zero handler reverts. The deterministic
seed forces nonzero protocol-fee accrual and collection in all six matrix cells,
a partial underfunded collection, and a pending withdrawal liability before
random actions begin.

Foundry printed its known non-fatal `unresolved symbol locals` diagnostic at
`src/spherex/SphereXProtectedRegisteredBase.sol:149` after compilation. Every
compiler and test command exited zero.

## Finalization required

The final receipt must replace the uncommitted test-patch hash with the exact
test/packet commit and signed release tag, rerun the commands from a clean
recursive checkout, retain their outputs, and pin the final mainnet plan and
reused dependency code hashes. The unresolved PeriodicTerm mainnet constants
remain a source-freeze gate.
