# R2 tracker: default and repayment implementation foundation

- Status: R2-01 and R2-02 complete; four size-reduction checkpoints in progress (S1 complete).
- [Spec](./v2.5-tranching-preparation.md) and [plan](./v2.5-tranching-preparation-r2-plan.md).
- Source baseline: `7038b5aa3f035355c773430eac1c24047bd4d7d7`.
- P-01 Solidity is committed at `a99c212`, signed kethcode. Release qualification remains outstanding.

## Tasks

| Task                                      | Status      | Acceptance / next action                                                                                                                                                        |
| ----------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R2-01 Working model and interfaces        | Complete    | Settled decisions recorded; auto-close working model, bounded allocation, callback treatment, ABI/storage and shared constraint discovery specified. Documentation checks only. |
| R2-02 Representative functional slice     | Complete    | 310 behavioral tests pass; two size gates fail as recorded below. User approved the source checkpoint; optimization follows functionality.                                      |
| R2-03 Creation terms and public surfaces  | Not started | All six combinations, structural safety, supported product limits and callback-safe terms.                                                                                      |
| R2-04 Default marker and run tracker      | Not started | Permanent marker with selected run/cure semantics and unchanged ordinary economics.                                                                                             |
| R2-05 Milestone qualification and R3 plan | Not started | Canonical suites and measured deployment evidence; user review/push; R3 scope prepared.                                                                                         |

## Decision register

Confirmed requirements are C-01–C-09 in the spec. In particular, default is a
marker for every newly deployed market, not a closure transition; accepted
withdrawals remain collectible without another eligibility veto.

| Proposal                                                      | State at documentation checkpoint                                                                                                                                                                      |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| P-01: 90-day run, observed cure and inclusive cutoff          | Selected 2026-09-25. Full cure at the cutoff counts, including a later transaction in the same block; latch an uncured default only afterward.                                                         |
| P-02: repayment completion and deadline test                  | Authorized working assumption: automatic funded closure. Prototype the existing full-funding criterion; preserve inclusive cutoff and historical default, with distinct donation/repayment accounting. |
| P-03: dated action gates and their duration                   | Selected: core deposit/borrow rejection and queue-hook bypass on every dated market; batching and sanctions preserved, followed by automatic funded closure.                                           |
| P-04: zero period, immutability, maturity relation and limits | Settled. Zero-day periods allowed with frontend warning; optional immutable terms, 90-day cap and fixed-maturity relation. Final network caps remain R5 work.                                          |

R2-01 selects the timestamp marker, retirement of execution dispatch, reserved
bit 93, effective-config rejection in the constructor, and the three minimal
events in the spec. Creation/getter selectors and the initial packed storage
layout are recorded there. Shared constraint discovery appends two repayment
bounds. R2-02 supplies initial runtime evidence for penalty/accounting mechanics
and bounded closure below; later qualification remains outstanding.

## R2-01 progress, 2026-09-25

- Verified the branch still points to signed documentation commit `192d0a1`
  with no tracked edits before resuming; the four user reference files remain
  untracked. No push or source changes from another session were found.
- Read the spec, plan, source interfaces, factory payload paths and callback
  configuration. Inspected the compiler's baseline revolving storage layout.
- Calculated the selected creation/getter selectors from the current ABI using
  the installed `ethers` library. No Solidity was edited and no new compiled
  feature size or runtime test result is claimed.
- P-01 selected by explicit user reply: "if they cure at the exact block, they
  cure at the exact block." Added same-block ordering and one-second-late
  acceptance timelines. A transaction at the cutoff must not foreclose a later
  timely cure in that block.
- The user distinguishes direct-transfer donations from repayment operations.
  Source and existing regression-test review confirms donations increase raw
  `totalAssets()` without minting shares or reducing revolving `drawnAmount`.
  Ordinary reserve/withdrawal accounting can use that liquidity. Do not treat
  a cash checkpoint as an explicit repayment operation.
- The user directed "assume autoclose for now." Automatic funded closure is
  the active working model; a further team answer is not a prerequisite to
  continuing. The draft's earlier continuation alternative is superseded.
- The user confirmed universal repayment-date action gates and reaffirmed that
  zero-day periods are allowed, with frontend documentation of the risk. They
  explicitly corrected reopening already settled questions. P-01 through P-04
  are now recorded without pending product questions.
- Selected the prototype's closure approach: reuse funded-close accounting,
  freeze fully backed claims, and finish FIFO allocation through bounded calls.
  Scheduled closure bypasses the vetoing close callback; periodic effective
  views/actions must consult core closure. Manual closure remains covered by
  its existing regression tests. R2-02 must prove these changes, not assume them.
- Markdown formatting, whitespace and local-link checks qualify this documentation
  checkpoint. No Solidity or runtime tests have been changed or run for it.

## Baseline evidence before R2-02

- Hook refactor M1–M5 completed at the baseline. The external M5 handoff retains
  the updated hook spec and qualified evidence. Its 749 reported tests in each
  final configuration do not qualify P-01.
- Existing stored-initcode margins: standard 1,597 bytes, revolving 932 bytes;
  open/fixed/periodic hooks 6,196 / 4,834 / 1,899 bytes. Cached deploy artifacts
  only; new feature size/gas was unmeasured at this checkpoint.
- Baseline market sources had no repayment terms or permanent default marker.
  Both APR execution paths already pass effective values to `_checkAprChange`.
- Existing queue and execution callbacks are separate. Execution defaults to no
  extra restriction in production templates but the core can dispatch a vetoing
  callback, which does not enforce the newly selected collection guarantee.
- Creation parameters and hook calldata have fixed assembly layouts. Keep the
  documented layout map and generated interfaces synchronized.

## Resume and checkpoint protocol

The user approved R2-02 for commit, then requested an optimizer-runs-1 size
measurement and a readthrough for existing bytecode techniques before further
optimization. Keep canonical runs at 44. R2-03 creation/public-surface
qualification remains next in the implementation plan. Do not reopen settled
product rules; ask only if new evidence exposes a material product conflict.

The requested size measurement and initial readthrough are recorded below.
The user then authorized all four size-reduction checkpoints, one at a time,
with a signed commit after each and review at the end. That authorization
replaces the precommit Solidity-review pause for these four tasks only.

At each task checkpoint, record the actual commit, changed behavior, tests,
size and remaining limitations here. Outside these four authorized optimization
tasks, stage Solidity for user review before committing. Use signed kethcode
commits. The user pushes. Keep all supplied
reference PDFs, the lifecycle sketch and the voice guide outside commits.

The earlier R2-01 checkpoint contains only Markdown. Its signed commit and
maintenance receipt are saved outside the repository. The R2-02 source receipt
records the tested file hashes and identifies the uncommitted review checkpoint.

## R2-02 progress, 2026-09-25

- Built the full core slice in an isolated checkout: optional terms, independent
  default tracking, shared view/write chronology, repayment gates, immediate
  penalty, automatic funded closure and bounded post-close batch allocation.
- Both factories pass the new terms. Supported templates enforce the selected
  bounds; fixed terms check maturity. Periodic effective views consult core
  closure, and the shared APR strategy preserves the repayment reserve floor.
- Zero-day periods remain valid. The exact cutoff is inclusive, including a
  later cure after another state update in the same block. No product rule has
  been reopened.
- Twelve focused behavior tests passed, including 1,000 fuzz cases. These cover
  both interest models, all six creation combinations, disabled/zero periods,
  exact and late cures, observed run reset with normal timer decay, late
  donations, frozen funded closure and bounded FIFO claim conservation.
- Two separately floored batch payments can leave one wei from aggregate
  backing. The conservation test checks paid claims plus residual cash, the
  two-batch floor bound, and that no scaled token or payable claim remains.
- Found and corrected a withdrawal-view mismatch: closure releases an existing
  batch before its original expiry, so its available-amount view must agree
  with execution. A fresh current batch still cannot execute before expiry.
  The new regression passes for both the released original batch and a fresh
  closed-market batch that is still current.
- Verified the compiled creation selectors against the spec. `MarketParameters`
  is 24 words (`0x300`); `MarketState` remains 14 words (`0x1c0`). Compiler layout
  places `_lifecycle` at slot 9, allowance at 10 and revolving principal at 11.
  Updated the explicit layout assertions and expanded borrower-transfer checks.
- Market, hook and factory regression selection: **298 passed, one failed**.
  The failure is the unchanged composition size check, not a behavior assertion.
  `WildcatMarketTest` passes all 82 cases, and expiry/donation and borrower-transfer
  suites pass. Source changes to existing tests update disabled-term literals,
  the 24-word parameter length, shifted storage slot and appended constraint
  discovery fields; they do not relax the original economic assertions.
- Separate prototype behavior selection: **12 passed**, including 1,000 fuzz
  runs. Combined: **310 passing behavior tests**. This is targeted evidence, not
  the full three-profile milestone qualification. Creation-adversary coverage,
  integration callback expectations and final consumer surfaces remain later tasks.

### Size priority and limits

The user directed functionality first, then a separate bytecode-reduction pass.
The measurements used the existing Solidity 0.8.25 / viaIR / optimizer-runs-44
configuration throughout. The first straightforward build was too large;
preventing compiler specialization removed duplicate transition bodies but did
not close the gap. An external-calculator experiment was archived and removed
from the active source. No external accounting dependency is selected.

Runtime and stored-initcode size remain release blockers. Functional runs may
use a larger test-only code-size allowance (the existing large test contracts
need it too). They exercise the actual factory paths, but do not prove network
deployability. Keep the separate artifact-size check and record its failure;
do not drop features to turn that check green. The plan now reflects this
ordering instead of its original prototype-size gate.

### Measured byte budget and commands

| Artifact                                 | Runtime bytes | Creation bytes | Stored-initcode headroom |
| ---------------------------------------- | ------------: | -------------: | -----------------------: |
| WildcatMarket                            |        25,272 |         27,008 |                   -2,433 |
| WildcatMarketRevolving                   |        25,855 |         27,665 |                   -3,090 |
| OpenTermHooks                            |        16,377 |         19,103 |                    5,472 |
| FixedTermHooks                           |        17,816 |         20,543 |                    4,032 |
| PeriodicTermHooks                        |        21,072 |         23,799 |                      776 |
| PeriodicTransferHooks (test composition) |        21,892 |         24,619 |                      -44 |

Runtime limit is 24,576 bytes. Stored creation code must leave room for the
leading STOP byte, so its payload limit is 24,575 bytes. Both markets exceed
both limits; the periodic test composition exceeds only the storage limit.
Both production factories remain within their applicable limits. These are
compiled measurements, not estimates.

Commands used for the final behavior checks:

```sh
FOUNDRY_PROFILE=deploy forge test --match-path 'test/{market,access,factories,integration/RepaymentPrototype.t.sol}/*.t.sol' --code-size-limit 262144 -vv
FOUNDRY_PROFILE=deploy forge test --match-path test/integration/RepaymentPrototype.t.sol --no-match-test test_ProductionArtifactsFitActualCodeStorageAndRuntimeLimits --code-size-limit 262144 -vv
FOUNDRY_PROFILE=deploy forge test --match-path test/integration/RepaymentPrototype.t.sol --match-test test_ProductionArtifactsFitActualCodeStorageAndRuntimeLimits --code-size-limit 262144 -vv
```

The first pattern selects market/access/factory suites; its fourth alternative
matches no path. The second command selects the 12 prototype behavior tests;
the third explicitly runs the excluded deployment-size gate and fails. The
unchanged composition size gate also remains failing. No size test was deleted
or weakened, and no canonical profile was changed. The 262,144-byte allowance is
only for behavior runs, including the large existing test harness. No network
deployability is claimed.

Full logs, ABI/layout checks, byte inventories and the parked calculator
experiment are in the external R2 handoff. The updated plan permits continuing
functional implementation before the bytecode-reduction pass. R2-03 and R2-04
must still qualify and land their explicit public-surface/default contracts;
R3 expands dated-accounting and boundary coverage. The user reviewed and
approved this source checkpoint for commit. The normal
integration callback expectations still describe execution-hook dispatch;
updating those intentional compatibility changes is part of R2-03, and the
full integration suite is not claimed to pass at this checkpoint.

## R2-02 size follow-up, 2026-09-25

The approved functional checkpoint is `a99c212b91bb04f86797069b335b6e1afcc9097b`,
with author/committer kethcode and its SSH signature verified. No push. All 24
Solidity files match the tested review receipt; the precommit documentation
change only recorded approval and the user's next instructions.

### Optimizer runs 1 versus 44

Built both settings from the same 65-file graph of market, factory, supported
hook and example-composition targets. Both builds succeeded. Compiler, source
hashes and metadata settings match apart from the run count. All ten targets
previously inventoried at 44 reproduce their prior runtime and creation sizes
exactly. The comparison is compile/size evidence; no runtime qualification at
runs 1 is claimed. `foundry.toml` remains unchanged at 44.

| Artifact                         | Creation at 44 | Creation at 1 | Bytes saved at 1 | Stored-initcode headroom at 1 |
| -------------------------------- | -------------: | ------------: | ---------------: | ----------------------------: |
| Standard market                  |         27,008 |        26,731 |              277 |                        -2,156 |
| Revolving market                 |         27,665 |        27,375 |              290 |                        -2,800 |
| Open hook                        |         19,103 |        19,027 |               76 |                         5,548 |
| Fixed hook                       |         20,543 |        20,463 |               80 |                         4,112 |
| Periodic hook                    |         23,799 |        23,788 |               11 |                           787 |
| Periodic transfer example        |         24,619 |        24,608 |               11 |                           -33 |
| Periodic borrow example          |         25,084 |        25,073 |               11 |                          -498 |
| Periodic APR-replacement example |         24,368 |        24,385 |              -17 |                           190 |

Market runtimes at 1 are 24,995 and 25,565 bytes: still 419 and 989 bytes over
24,576. The stored creation-code limit remains tighter. The expanded inventory
also identifies the periodic borrow example's 509-byte storage overage at 44;
the earlier test stopped at the transfer example's first size failure. Factories
and the supported production hook templates remain within their own limits.

Runs 1 is a useful comparison, not a guaranteed minimum. The APR-replacement
example grows by 17 creation bytes and 27 runtime bytes. The old sweep's record
at `df688a9` says it had already tested runs 1 through 200 on that earlier source;
this measurement does not replace a new sweep if later needed.

The original full-test-graph runs-1 compilation was stopped before completion
to avoid compiling test harnesses for a size-only question. Its log is retained
but provides no successful result. The completed comparison uses these targets
for both builds:

```sh
FOUNDRY_PROFILE=deploy forge build \
  src/market/WildcatMarket.sol src/market/WildcatMarketRevolving.sol \
  src/HooksFactory.sol src/HooksFactoryRevolving.sol \
  src/access/OpenTermHooks.sol src/access/FixedTermHooks.sol \
  src/access/PeriodicTermHooks.sol test/mocks/TransferFeatureHooks.sol \
  test/mocks/BorrowFeatureHooks.sol test/mocks/AprReplacementHooks.sol \
  --skip test --skip script --optimizer-runs 1 \
  --out /tmp/wildcat-r2-runs-1-out --cache-path /tmp/wildcat-r2-runs-1-cache \
  --build-info
```

Repeat with runs 44 and separate output/cache paths for the control. Actual
commands, artifact hashes, build-info and all 16 measured targets are retained
in the external handoff's `r2-02-optimizer-runs-comparison.json` and build records.

### Readthrough: reuse existing techniques first

These are candidates, with no claimed byte savings until individually measured:

1. **Avoid redundant memory allocation.** Optimized market IR contains seven
   discarded struct-zeroing calls, including four in the new state-return
   wrappers. The nested `LifecycleTransition` initializer also allocates objects
   that are replaced immediately; accrual records are allocated twice. Extend
   the existing [pointer-return technique](../../src/libraries/FunctionTypeCasts.sol),
   and consider one explicitly initialized workspace with reusable record slots.
2. **Share withdrawal-payment arithmetic.** Both payment functions in
   [WildcatMarketBase](../../src/market/WildcatMarketBase.sol) remain separate in
   optimized IR. Share the calculation and return deltas for the event-emitting
   path. Preserve exact floor rounding, zero-payment behavior and event order.
3. **Use the existing event/error encoders.** New lifecycle events/errors use
   ordinary Solidity encoding. [MarketEvents](../../src/libraries/MarketEvents.sol)
   and [MarketErrors](../../src/libraries/MarketErrors.sol) provide compact
   precedents. Accrual snapshots already contain the six event words in order;
   logging that buffer could avoid reconstructing it during replay.
4. **Combine safe scalar predicates.** Apply
   [BoolUtils](../../src/libraries/BoolUtils.sol) only where both operands are
   normalized booleans and eager evaluation preserves behavior. Retain essential
   short-circuiting, especially around external calls and potentially reverting
   expressions.
5. **Tighten internal terms/getter handling.** Compare one internal decoder and
   compact external wrappers. A terminal assembly `return` must not be copied
   into a public getter that is called internally. Keep callback-safe reads and
   the constructor-proved timestamp bounds.
6. **Review temporary integer widths.** Narrow fields occupy whole memory words
   and add masks/counter checks. Measure wider internal snapshot/counter fields;
   preserve packed lifecycle storage, `MarketState`, hook ABI and checked
   accounting bounds.
7. **Reduce hook setup/query overhead.** Creation constructs the same constraint
   tuple twice. Try one shared snapshot without weakening override/discovery
   behavior. Periodic closure-query encoding is another fixed-buffer candidate,
   with revert, return-length and bool-validation semantics preserved.

The earlier `_runtimeConstant` work is already effective: optimized IR has one
transition body and one batch-commit body per market. The now-unused
`_processExpiredWithdrawalBatch` is already absent from optimized IR, so deleting
it would clean up source without saving deployed bytes. Factory-only reductions
also do not shrink the separately stored market creation code.

Start with allocation and arithmetic sharing, then the smaller encoding and
predicate candidates. Measure at canonical runs 44 and retain focused behavior,
ABI/storage and gas evidence. External calculators or deployment-storage changes
remain later options; no architectural change is selected by this readthrough.
The full report and IR evidence are preserved outside the repository.

## Four size-reduction checkpoints, 2026-09-26

Canonical compilation remains Solidity 0.8.25, Cancun, viaIR and optimizer
runs 44. Preserve behavior, public ABI, packed storage, rounding and event
order. Measure each checkpoint against its predecessor and the R2-02 baseline;
reject candidates that grow code. No calculator or deployment architecture
change is included. The user reviews all four signed commits together.

| Task | Status      | Scope / acceptance                                                                                                                                            |
| ---- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S1   | Complete    | Reuse already allocated accrual records. Larger workspace/pointer-return candidates were rejected. Four-boundary replay and view/write agreement are covered. |
| S2   | Not started | Share withdrawal-payment arithmetic; preserve exact floor rounding, accounting and zero-payment/event behavior.                                               |
| S3   | Not started | Apply existing compact event/error encoding; compare topics, complete data and revert payloads.                                                               |
| S4   | Not started | Measure safe scalar-predicate and related local overhead reductions; retain short-circuiting where required and all hook override behavior.                   |

Each checkpoint gets focused behavior tests, a matched artifact comparison and
an ABI/storage-layout comparison. Deployment-size gates remain independent of
the larger test-harness allowance. This work does not complete R2-03, R2-04 or
full release qualification.

Evidence is retained outside the repository at
`/home/kethcode/wildcat/tranching-size-review/2026-09-26/`: matching 65-source
builds, optimized IR, size/ABI/layout inventories, rejected candidate patches
and test command/log records. The baseline reproduces the committed R2-02
sizes exactly. The four user-owned reference files remain untracked.

### S1: reuse accrual records

`LifecycleTransition` already allocates four `LifecycleAccrual` records. Fill
the next record in place instead of allocating another and replacing its
pointer. This removes one allocation per actual accrual interval without
changing the struct layout, public returns or chronology.

The more ambitious return-pointer and explicit-workspace candidates were
rejected: measured creation growth ranged from 23 to 2,071 bytes per market.
Optimized IR exposed duplicated transition code in the larger variants.
The custom allocator and its experimental memory test are archived only;
no new assembly allocator ships in S1.

The retained change saves **11 runtime and 11 creation bytes per market**.
Standard creation is 26,997 bytes; revolving creation is 27,654. Their stored
creation overages remain 2,422 and 3,079 bytes. All measured hooks, factories
and composition examples are byte-identical to the baseline. Public ABIs and
normalized storage layouts match for every inventoried target.

The added regression crosses repayment date, batch expiry, deadline and the
current timestamp in one update for both market models. It checks all four
accrual intervals, repeated views, persisted view/write agreement and the
historical default timestamp. Existing economic assertions are unchanged.

Validation: **121 focused tests passed**, with fixed seed `0x5eed` and 1,000
runs per fuzz test, using the deploy profile and the existing test-only size
allowance. This includes market, historical-checkpoint, borrower-transfer,
repayment and event-encoder suites. Prettier and `git diff --check` pass for
the changed files. Evidence labels: `step1e` (accepted), `step1` through
`step1d` (rejected experiments). User authorized this signed source checkpoint
and review after all four steps.
