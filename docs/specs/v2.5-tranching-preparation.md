# V2.5 tranching preparation: repayment and default

- Status: implementation draft; no P-01 Solidity changes yet.
- Source baseline: `7038b5aa3f035355c773430eac1c24047bd4d7d7`.
- Scope: new V2.5 standard and revolving markets, with open, fixed or periodic hooks.
- Next milestone: [R2 plan](./v2.5-tranching-preparation-r2-plan.md) and
  [tracker](./v2.5-tranching-preparation-r2-tracker.md).

V2.5 needs a permanent default marker and optional dated repayment obligations
before deployment. The completed hook refactor provides the extension points
for a later tranche policy. V2.6 will supply the tranche vault, senior/junior
shares, waterfall and tranche-specific validation.

This is a working specification, not a description of deployed behavior or an
audit attestation. Confirmed requirements are separated from product proposals
and engineering recommendations. Committing the draft does not approve every
proposal below. Archive this document and its plan/tracker outside the repository
before the final release; retain the resulting behavior in maintained docs.

## 1. Confirmed requirements

| ID   | Requirement                                                                                                                                                                                                     |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C-01 | Every newly deployed V2.5 market type has a permanent default marker, including markets without repayment terms.                                                                                                |
| C-02 | Default is a state marker. It does not itself close the market, accelerate repayment, change rates or open withdrawals. No default-reason field is required. Bool versus timestamp is an implementation choice. |
| C-03 | At the repayment date, require 100% reserves and start penalty immediately when underfunded. The requirement must take effect without a voluntary borrower parameter update or sufficient funding.              |
| C-04 | A later rate/reserve update cannot undo the dated 100% requirement. Existing funding checks already prevent APR updates while underfunded on both APR routes. Additional APR restrictions can live in hooks.    |
| C-05 | Repayment/default preserves batch durations, expiry keys, pro-rata allocation and FIFO payment order. It does not turn withdrawals into immediate individual payments.                                          |
| C-06 | Once withdrawal is accepted, collection of the payable claim must not introduce a second hook eligibility veto. Keep expiry, funding, claim accounting and market sanctions handling.                           |
| C-07 | The borrower initiates APR changes. Hooks see current/requested/effective market values and validate or reject them. A future tranche policy can read the vault's senior APR through its own interface.         |
| C-08 | Delinquency and penalty cause no additional tranche behavior. There is no selected distress observer, tranche valuation freeze or extra tranche deposit gate.                                                   |
| C-09 | An already-recorded default does not grant another repayment/grace interval and lead to a second default cycle. The marker stays set through cure and funded closure.                                           |

The marker applies to newly deployed contracts. Existing deployed markets remain
immutable. Repayment is distinct from `isClosed`: current funded closure settles
all debts, stops future accrual and rejects further repayment. Marking unpaid
debt as `isClosed` would change those behaviors and is not the selected design.

The user's newer clarification of C-02 supersedes the earlier description of
default as closure. C-03 supersedes the explanation PDF's ordinary-grace wording.
The other session's handoff is useful input, not blanket approval of its claims.

## 2. Product decisions to close before dependent implementation

The following form one concrete working model for the timelines and tests in
this draft. They remain proposals. Resolve them at R2-01, one at a time if
discussion is needed; record the selected answer before dependent production
code is accepted. Do not reopen C-01 through C-09.

| ID   | Working proposal                                                                                                                                                                                                                                                                                    | Consequence to review                                                                                                                                                                                                                      |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| P-01 | Record default after an uncured 90-day penalized episode. A healthy state write resets this separate run. Use an inclusive cure cutoff: writes at the cutoff timestamp can cure; seal default only at a later timestamp. Count the penalized condition even if the configured penalty rate is zero. | This is a new run tracker, not a reinterpretation of `timeDelinquent`. The inclusive cutoff aligns a 90-day run with an inclusive 90-day repayment period. Confirm the threshold, reset and equality semantics explicitly.                 |
| P-02 | The repayment deadline is inclusive. Evaluate recognized assets and all debt as of that cutoff. Exact full funding is sufficient; funded closure is not additionally required. Seal a missed deadline on the first successful update strictly after it.                                             | A temporary surplus before the deadline is insufficient if debt grows beyond it. A token transfer unobserved by any market update before the cutoff does not count as timely. Later collection of allocated funds must not create default. |
| P-03 | Every market configured with repayment terms rejects deposits and borrowing from the date and bypasses queue-hook admission restrictions. Corresponding live capacity/borrowing views report zero.                                                                                                  | This is a universal dated-market promise, rather than only a tranche-template rule. Sanctions and normal batches remain. C-06 independently protects collection of accepted claims.                                                        |
| P-04 | Terms are optional and immutable at creation. Permit a zero-length period, with a template cap of 90 days. Fixed-term hooks require the date at or after maturity. Open/periodic date caps initially use two years as a placeholder.                                                                | The final caps, the proposed fixed-term cap increase, and existing periodic mainnet constants need release decisions. Caps are template product policy, not borrower-selected safety overrides. Zero period needs clear UI treatment.      |

P-02 and P-04 imply that a zero-period facility may repay throughout the date's
timestamp; an unfunded deadline is judged only after that timestamp. Rejecting
zero periods is a valid alternative until P-04 is selected. Do not implement
both a nonzero-period requirement and supposed zero-period support.

## 3. Terms, state and authority

Use `R` for repayment date, `P` for repayment period and `D = R + P` for the
deadline. These are independent of fixed maturity and periodic withdrawal
windows. A fixed market may become open term before `R`.

### Creation and validation

Proposed creation fields are `uint32 repaymentDate` and `uint32 repaymentPeriod`,
appended to both `DeployMarketInputs` and `MarketParameters`. Both factories pass
them to the hook and the market constructor. Field names/types are the working
ABI proposal; R2 records the exact selector and encoding changes.

Core validation is independent of the installed hook:

- `repaymentDate == 0` requires `repaymentPeriod == 0` and disables dated behavior.
- A nonzero date must be in the future. Under P-04, period zero is valid here.
- Add date and period in a wide type and require the result to fit `uint32`.
- Borrower/principal and hook-administrator transfers cannot mutate these terms.

Hooks enforce product limits using the existing parameter-validation structure.
Fixed-term initialization also checks `repaymentDate >= fixedTermEndTime` when
terms are enabled. Existing maturity updates can only move maturity earlier,
so they preserve that relation; retain a regression for this fact.

The preferred initial configuration keeps limits in the registered template's
policy and preserves the existing `(administrator, bytes args)` constructor
envelope. Sepolia and mainnet may register policies with different approved
limits. If immutable deployment configuration replaces constants, specify its
authority first: current factory constructor bytes come from the deployment
caller. Immutability alone does not prevent a borrower choosing arbitrary caps.
Never silently add positional constructor arguments without updating both
factories, deployment tooling and tests.

Constraint discovery must report the limits that the selected template actually
enforces. Update `getParameterConstraints` or its replacement alongside the
validation logic; do not leave a base-constant getter advertising different
bounds from the deployed policy.

### Default marker and storage

Prefer a `uint32 defaultedAt` marker, with zero meaning no recorded default and
an optional boolean getter derived from it. This provides a historical cutoff
without a separate reason enum. A boolean plus an event is an allowed measured
alternative; finalize storage/API in R2.

Keep lifecycle bookkeeping outside `MarketState`. It is copied verbatim into
hook calldata with size `0x1c0`. The market constructor also has a fixed response
size, currently `_MARKET_PARAMETERS_SIZE = 0x2c0`; two appended static fields
would make it `0x300`. Update every encoder, decoder and assembly dependency
together rather than changing only the Solidity declarations.

Pack the marker, run tracking and any needed completion flags into ordinary
storage where possible. Avoid the five reserved top-of-storage address slots
and map effects on inherited revolving storage. Conceptual flags may be derived
from already-persisted timestamps if tests prove event uniqueness and correctness.
Do not add a large phase enum or history log without a demonstrated requirement.

No administrative `setDefault` or clearing function is proposed. A marker is
latched once, using the earliest qualifying historical cutoff. Subsequent
causes, cure, authority transfers and closure do not overwrite it. Default
storage must never become an authorization gate for unrelated market actions.

## 4. Actions and transitions

This table uses P-01 through P-04. “Ordinary” means existing market/term-hook
behavior, including access, reserve checks and sanctions. Default itself is
orthogonal to the columns and changes none of the actions.

| Action                           | No terms, or before R                                                | From R while not closed                                                             | Funded closure                                      |
| -------------------------------- | -------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | --------------------------------------------------- |
| Deposit / maximum deposit        | Ordinary                                                             | Reject / zero under P-03                                                            | Existing rejection / zero                           |
| Borrow / borrowable assets       | Ordinary                                                             | Reject / zero under P-03, including surplus draws                                   | Existing rejection / zero                           |
| Queue withdrawal                 | Ordinary hook admission and batching                                 | Bypass queue admission hook under P-03; keep sanctions, balances and normal batches | Existing closed-market batching; preserve sanctions |
| Execute allocated withdrawal     | C-06: no second eligibility veto; ordinary expiry/funding/accounting | Same; no early batch expiry                                                         | Same existing funded-claim accounting               |
| Repay / bounded batch processing | Ordinary                                                             | Continue; preserve FIFO and explicit revolving principal treatment                  | Existing closed-market restriction                  |
| APR update, both routes          | Existing term policy and funding guards                              | Preserve 100% reserves; funding guard and term policy still apply                   | Existing rejection                                  |
| Collect protocol fees            | Ordinary                                                             | Preserve existing fee priority and liability accounting                             | Ordinary funded-close accounting                    |
| Funded `closeMarket`             | Existing borrower authority and closure hook                         | Still requires full funding and ordinary closure authorization                      | Cannot close twice                                  |
| Authority transfer               | Existing authorization                                               | Existing authorization; immutable terms unchanged                                   | Existing behavior                                   |

### Scheduled reserve and penalty transition

The core applies the 100% requirement internally. It cannot call the ordinary
borrower-only setter, whose funding checks would prevent this transition when
it is needed. No new general borrower authority is granted to the hook.

The dated requirement is effective at `R`, even when the first later transaction
is delayed. Views calculate the same effective state. A successful write emits
the transition once with timestamp `R`; a reverted transaction records nothing.
Closed facilities do not restart accrual when their former date arrives.

Preserve the current penalty base, fee separation and rounding in
[`FeeMath`](../../src/libraries/FeeMath.sol). Do not switch to charging only the
cash shortfall, double-charge an already-penalized interval, or charge funded
withdrawal liabilities as though they were live interest-bearing shares.

Engineering recommendation: keep ordinary `timeDelinquent` advancement/decay,
and treat the full duration of an underfunded interval at/after `R` as penalized,
without waiting for grace. Healthy intervals retain ordinary timer/fee decay;
funded closure retains its existing stopping behavior. This avoids fabricating
pre-date delinquent time. Prove the result for both interest models before
selecting the helper implementation. Simply zeroing grace everywhere or raising
the timer can change healthy-tail economics; neither is an interchangeable fix.

Bundled APR strategies must also handle the dated reserve floor. A pre-date
temporary-reserve entry must not restore a lower ratio, and should not make an
otherwise permitted, fully funded APR update fail indefinitely. Compute the
correct effective ratio before `_checkAprChange`, retire obsolete temporary
bookkeeping consistently, and retain a core floor check. Do not silently clamp
the result after the validator has seen different “effective” values.

### Withdrawal collection

C-06 applies to accepted claims generally, including markets without repayment
terms. The preferred implementation removes execution-hook dispatch from new
markets. Preserve its bit position/interface declaration as reserved if needed
for compatibility, and reject an effective creation config that claims the
unsupported execution callback is enabled. Record the exact ABI/capability
treatment in R2. This is an engineering proposal, not an implemented change.

An unconstrained callback with a caught revert is not a proven substitute: it
can still consume gas or make payment depend on external behavior. No new
observer or bookkeeping callback is required by the current scope.

The market still validates batch/claim accounting and routes a sanctioned
recipient's payment to escrow. Direct queueing retains `_getAccount` sanctions
checks. Quarantine and token-transfer handling are distinct from collection;
do not remove their checks as a side effect of changing withdrawal dispatch.
Under P-03, bypass of the queue hook intentionally removes its schedule, access
and feature checks after `R`; future policies cannot depend on that callback
for mandatory post-date accounting.

## 5. Default clock and repayment outcome

Under P-01, maintain a separate start/cutoff for an uncured penalized episode.
It is not `timeDelinquent`: that timer rises during delinquency and decays while
healthy, and can keep producing penalty fees during healthy decay.

- Before `R`, an uncured episode starts when ordinary grace has been exhausted.
- At/after `R`, an underfunded interval is immediately eligible. An episode
  already running continues; reaching `R` never restarts it.
- An observed healthy write resets only this default-run tracker. It does not
  forgive fees, reset the economic timer or clear a recorded default.
- Under the proposed inclusive cutoff, a healthy write at the cutoff can cure;
  the first later update records any earlier uncured expiry before processing
  a late cure. Multiple transactions at the cutoff must get the same opportunity.
- Snapshot/update rules also cover batch-boundary classification and zero-value
  maintenance calls. Historical transitions must not be erased by the final
  healthy classification of a later action.

The tracker must observe the existing economic intervals without splitting
interest accrual merely to emit a default marker. Adding an otherwise unnecessary
split could change compounding/rounding on markets without repayment terms.

Under P-02, evaluate the debt at `D` once. At 100% reserves, current
`liquidityRequired()` equals `totalDebts()`: live normalized supply, allocated
unclaimed withdrawals and accrued protocol fees. Unpaid queued shares are
already in live supply. Do not double-count them, require funded claims to have
been collected, or add an arbitrary dust tolerance.

If the facility was fully funded and closed earlier, it satisfies the dated
obligation. Otherwise compare recognized assets with the exact debt through
`D`. An earlier temporary surplus is not sufficient. Full funding at `D` does
not automatically close the market or stop later interest; the dated cutoff
is not reevaluated using later balances. The ordinary default-run rule continues
to apply to later underfunding. Any earlier recorded default remains recorded.

For a timestamp marker, use the missed cutoff timestamp (`D`, or the episode
cutoff), while latching it only once that inclusive cutoff can be evaluated.
The timestamp identifies the missed obligation, not the block in which the
storage write happened. Do not add one to a `uint32` maximum deadline merely
to represent the first later block; preserve the existing finite time horizon.

## 6. Chronological accounting

The shared transition design must serve both `_getUpdatedState` and
`_calculateCurrentState`. Share calculations where practical; only the write
path commits storage/events. Derive timestamps from terms and interval state,
not from the keeper's arrival time. Keep work bounded, independent of elapsed
days; existing explicit bounded withdrawal processing remains separate.

### Historical cash and principal

[`_writeState`](../../src/market/WildcatMarketBase.sol) currently retains its
asset checkpoint only while a current batch exists. Extend recognized asset
tracking for enabled repayment terms, including with no pending batch. Snapshot
the final post-action balance on successful writes, with the existing saturation
bound or an equally justified representation.

Both `repay` and `repayAndProcessUnpaidWithdrawalBatches` transfer funds before
state update. Use historical recognized assets to evaluate crossed dates, and
current assets to fund today's batches. Today's cash must remain available today
without being credited to yesterday's deadline. Update the checkpoint only after
the relevant historical evaluation. Direct ERC-20 transfers are recognized at
a market update; the contract cannot infer their earlier transfer timestamps.

Use matching historical debt, scale, fees and revolving drawn principal.
Revolving donations remain liquidity rather than explicit principal repayment.
Reconcile today's explicit repayment after the historical intervals it cannot
change. Reuse current commitment-fee/drawn-APR economics; do not replace them
with the PDF's shorthand commitment fee on undrawn capital.

### Boundary ordering

The first implementation milestone must lock and test the following ordering:

1. Accrue each interval through its next logical boundary under the regime that
   applied during that interval. Derive default-run crossings from the same
   historical classifications, preserving inclusive-cure semantics under P-01.
2. Apply the repayment requirement at `R`; subsequent underfunded seconds have
   no grace. There is no fee for a zero-length interval at the transition itself.
3. Preserve batch expiry's existing strict comparison: expiry `E` is processed
   when `now > E`, not `now == E`. At a coincident `R`, activate the dated rule
   before post-boundary classification; retain normal batch payment arithmetic.
4. Evaluate an inclusive deadline from the state as of `D`, incorporating all
   recognized writes timestamped at/before it and only batch expiries strictly
   earlier than `D`. A batch whose expiry equals `D` must not be treated as
   already expired by a view of `D`. Evaluate its expiry afterward on catch-up.
5. Apply the current action and its cash/principal changes after earlier cutoffs
   have been judged. Writes exactly at an inclusive cutoff remain eligible;
   finalization waits for a later timestamp. Keep events and marker timestamps
   consistent with those rules.

When `R == D`, activation and deadline observation have no intervening accrual
interval, but all writes at that timestamp still count under P-02. Default does
not depend on which transaction was first in the deadline second. If the
reference model reveals incompatible tie rules, revise them explicitly before
production integration; do not rely on call order to choose economics.

### Worked acceptance timelines

These are specifications, not executed test results. Zero APR/fees isolate
cash timing in the first three examples.

| Case                                                                      | Expected result under the working model                                                                                                                                   |
| ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Debt 100, recognized cash 20 before D; first later call repays 80 at D+1  | Current cash becomes 100 and can fund batches; historical deadline remains missed.                                                                                        |
| Same state; repay 80 through the market at D                              | That write counts. A first update at D with cash 20 must not prematurely finalize default before the repayment in the same timestamp.                                     |
| Direct transfer 80 at D without any market observation until D+1          | Transfer helps current liquidity, but historical recognized cash is still 20. Explain this consequence to integrators.                                                    |
| R=D, first later call at D+1, with historical shortfall                   | Activate at R, record the missed cutoff, then accrue the post-R interval once. Preserve normal batches.                                                                   |
| Penalty episode cutoff equals D, and a healthy repayment is recorded at D | Under proposed P-01/P-02 equality, neither trigger may defeat the timely cure. If a different run equality rule is selected, this expected result must change explicitly. |
| Default recorded before R                                                 | Marker stays set; no accelerated date, new grace or special withdrawal opening. The originally scheduled R still applies on its own date.                                 |
| Fully funded unclaimed withdrawals at D; lenders collect afterward        | Their delayed collection does not create or erase a missed deadline. Preserve exact existing settlement rounding.                                                         |

## 7. Public interfaces and consumer impact

Prefer small, explicit interfaces over an extensible lifecycle framework.
The following names are proposed ABI, to be finalized with R2's inventory.

| Surface                                                                                        | Proposed contract                                                                                                                                                                                            |
| ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `repaymentDate()`, `repaymentPeriod()`, `repaymentDeadline()`                                  | Static terms, returned as `uint256`, with zero for disabled terms. Safe to read from callbacks; no `nonReentrantView` guard or external calls.                                                               |
| `defaultedAt()` and optionally `isDefaulted()`                                                 | Committed marker only, with clearly documented lazy persistence. No reason enum or public setter. Readable during callbacks as committed storage, not a promise about intermediate state.                    |
| Live market/borrowing/capacity views                                                           | Reflect the dated regime even before a transaction records it. Preserve current view guards. Do not silently expose a stored marker as a computed historical preview.                                        |
| Factory inputs and `IHooks.onCreateMarket`                                                     | Append repayment fields through both factory paths and creation tuples; regenerate selectors and dependent encoders. Hooks receive terms before the market exists and cannot query its code during creation. |
| `RepaymentDateReached(uint256 effectiveTimestamp)`                                             | Emitted once on successful activation, with R rather than the later transaction timestamp. No activation event for a facility closed before R.                                                               |
| `DefaultRecorded(uint256 effectiveTimestamp)`                                                  | Emitted once when the permanent marker is first committed; no reason field. Boolean storage alternatives must retain equivalent event semantics.                                                             |
| `MarketRepaymentTerms(address indexed market, uint256 repaymentDate, uint256 repaymentPeriod)` | Dedicated factory event for both enabled and disabled terms, emitted by both factories. Prefer this to changing existing deployment-event signatures unnecessarily.                                          |

Penalty-run start/reset events and a historical observation API are not required
in the initial implementation. Existing batch/accounting events remain the
source for ordinary settlement replay. Any later default preview must be named
separately, have explicit reentrancy semantics and match the write computation.

Update core/live/aggregator lenses and their interfaces deliberately. Keep
existing current/stored-state distinctions. A size-driven move to a lens cannot
remove the simple term getter a hook needs during its callback.

Consumers must distinguish repayment, default and closure. Subgraphs index the
new terms and permanent marker; SDKs update tuple encoding, selectors, types and
factory addresses; the app distinguishes queued versus collectible amounts and
explains any selected zero-period/direct-transfer rules. Existing template and
market deployment identities change where bytecode changes. Preserve old
deployment records; do not relabel them as the new source.

The existing `_checkAprChange` inputs are sufficient for the future tranche
policy. Senior rate/floor binding, a sole-lender vault, quarantine protection,
tranche shares and waterfall integration remain V2.6 work. No actual vault
integration is claimed by this specification or the completed hook tests.

## 8. Source map and deployment budget

| Area                                                                                                                                                                                                                    | Change boundary                                                                                                     |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| [Creation structs](../../src/interfaces/WildcatStructsAndEnums.sol), [standard factory](../../src/HooksFactory.sol), [revolving factory](../../src/HooksFactoryRevolving.sol) and interfaces                            | Terms, temporary constructor storage, ABI/assembly encoding, events and both real deployment paths                  |
| [Market base](../../src/market/WildcatMarketBase.sol), [standard actions](../../src/market/WildcatMarket.sol), [revolving accounting](../../src/market/WildcatMarketRevolving.sol)                                      | Constructor validation, marker/run storage, date/deadline processing, checkpointing, views and historical principal |
| [Rate config](../../src/market/WildcatMarketConfig.sol), [withdrawals](../../src/market/WildcatMarketWithdrawals.sol), [FeeMath](../../src/libraries/FeeMath.sol)                                                       | Reserve floor, dated gates, non-vetoable collection and penalty interval calculation                                |
| [Base hooks](../../src/access/BaseHooks.sol), [constraints](../../src/access/MarketConstraintHooks.sol), term policies and [callback encoding](../../src/types/HooksConfig.sol)                                         | Creation validation, selected limits, effective APR/reserve consistency and callback capability treatment           |
| [Events/errors](../../src/interfaces/IMarketEventsAndErrors.sol), [core lens](../../src/lens/MarketLensCore.sol), [live lens](../../src/lens/MarketLensLive.sol), [aggregator](../../src/lens/MarketLensAggregator.sol) | Custom emitters, public terms/default reads, tuple changes and downstream interpretation                            |

Cached qualified deploy artifacts use Foundry 1.8.3, Solidity 0.8.25, Cancun,
via IR, optimizer 44 runs and no CBOR metadata. These are existing sizes, not
measurements of P-01:

| Contract               | Runtime bytes | Creation bytes | Runtime headroom | Stored-initcode headroom |
| ---------------------- | ------------: | -------------: | ---------------: | -----------------------: |
| WildcatMarket          |        21,547 |         22,978 |            3,029 |                    1,597 |
| WildcatMarketRevolving |        22,125 |         23,643 |            2,451 |                      932 |
| OpenTermHooks          |        15,653 |         18,379 |            8,923 |                    6,196 |
| FixedTermHooks         |        17,014 |         19,741 |            7,562 |                    4,834 |
| PeriodicTermHooks      |        19,949 |         22,676 |            4,627 |                    1,899 |

[`LibStoredInitCode`](../../src/libraries/LibStoredInitCode.sol) stores
`STOP || creation code` as runtime code. That extra byte must fit the 24,576-byte
limit too. Passing the market runtime-size limit alone does not prove deployment.
Factory runtime headroom is 8,018/7,212 bytes in the same cached artifacts.

R2 must measure a representative vertical slice containing terms, tracking,
dated accounting and required gates, then deploy both market models through
their actual factories. Measure final complete code again before qualification.
Do not use a stub-only size result to claim the feature fits. If it does not fit,
record a concrete reduction or storage/deployment redesign for review before
expanding implementation. No extra-gas estimate is qualified yet.

## 9. Acceptance and release gates

| ID   | Required evidence                                                                                                                                                                                                                                   |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A-01 | Both factories deploy every standard/revolving × open/fixed/periodic combination, with enabled and disabled terms; constructor safety holds even with a permissive hook.                                                                            |
| A-02 | No-date markets retain existing economics, action authorization and batching, with explicit exceptions for new default visibility and C-06's execution guarantee. Preserve tests for existing behavior instead of rewriting expectations wholesale. |
| A-03 | R/D handling agrees between views and writes with no calls near boundaries, late first calls, direct transfers, explicit repayment and no pending batch.                                                                                            |
| A-04 | Inclusive-cutoff tests cover multiple same-timestamp writes, zero period, exact funding, one-unit shortfall, early funded closure and later withdrawal claims.                                                                                      |
| A-05 | Default-run tests distinguish `timeDelinquent` decay from the new clock; cover cure, renewed delinquency, late cure, zero penalty rate, threshold equality and both triggers together. Marker never clears or changes existing permissions.         |
| A-06 | Both APR paths preserve 100% reserves; funded/underfunded updates, temporary-reserve expiry/cancellation and effective-value validation are covered.                                                                                                |
| A-07 | Accepted claims remain collectible despite a reverting execution policy. Queue policy still applies before R, selected bypass applies afterward, sanctions escrow remains, and partial payments cannot overpay or be claimed twice.                 |
| A-08 | Batch expiry/order/rounding and bounded FIFO processing remain correct, including equality with R/D and current cash that must not receive historical credit.                                                                                       |
| A-09 | Revolving principal, donations, explicit repayments, commitment fees and historical utilization are preserved across every new boundary.                                                                                                            |
| A-10 | Default/event uniqueness, rollback, effective timestamps, creation tuples, term getters during callbacks, authority transfers and lenses match the selected interfaces.                                                                             |
| A-11 | Stateful tests enforce permanent markers, reserve floors, chronological outcomes, payable-claim conservation, no duplicate penalties and no retroactive cure. Reference expectations must be independent of production helpers.                     |
| A-12 | Full canonical suites, formatting/lint, ABI/layout/identity inventory, gas evidence, stored-initcode sizes and real factory deployment qualify the final source.                                                                                    |

Use the canonical commands in [TESTS.md](../../TESTS.md). Test counts from M5
qualify the hook refactor only; this feature needs its own results. SDK/app/
subgraph checks must name what was actually run, with outstanding external
consumer work recorded rather than implied complete.

The current [audit scope](../releases/v2.5-audit-scope.md) pins `7fad3de`.
`a1575b1` changed the expiry asset checkpoint after that source; the hook refactor
and P-01 are additional deltas. Prepare a new inventory, source freeze and
review packet covering all three. Do not overwrite the old receipt or claim
the prior audit covers the new code. Finalize network constants, update maintained
docs, export the updated spec and remove these temporary working documents.

The next implementation work is R2, after the maintenance pause and resolution
of the explicitly marked product choices. Solidity edits are staged for user
review before signed task commits. No push or deployment is part of this draft.
